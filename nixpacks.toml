[phases.setup]
nixPkgs = ["nodejs_20", "python311"]

[phases.build]
cmds = [
  "cd frontend && npm install && npm run build && cd ..",
  "pip install --upgrade pip && pip install -r requirements.txt"
]

[start]
cmd = "PYTHONPATH=. uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
